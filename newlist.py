# Initialize... over...

import tkinter
from tkmacosx import Button

from tkinter import *

root = Tk()
root.geometry("800x600")
root.title("Roberto 0.1")
root.resizable(False, False) # Prevents both horizontal and vertical resizing

root.mainloop()

